<br><br><br>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3 class="text-center">精准帮扶</h3>
    </div>
    <br><br><br>
<div class="col-md-10">
    <table class="table table-hover table-responsive">
      <tbody>
        <% @aids.each do |aid| %>
          <tr>
            <td>
              <%= aid.id %>
            </td>
            <td>
              <%= link_to(aid.title, aid_path(aid)) %>
            </td>

          </tr>
        <% end %>
      </tbody>
    </table>
</div>
  </div>
</div>
<div class="text-center">
  <%= paginate @aids %>
</div>
