<%= simple_form_for [:admin, @judgment] do |f| %>

  <div class="group">
    <%= f.input :title, :label => "识别名称" %>
  </div>

  <% if @judgment.image.present? %>
   <span>目前图片</span>

   <%= image_tag(@judgment.image.thumb.url) %>
  <% end %>

  <div class="group">
    <%= f.input :image, as: :file %>
  </div>

  <div class="group">
    <%= f.input :description, as: :ckeditor, input_html: { ckeditor: { toolbar: 'mini' } }, :label => "内容" %>
  </div>


  <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>

<% end %>
