<%= simple_form_for [:admin, @village] do |f| %>

  <div class="group">
    <%= f.input :title, :label => "新农村项目名称" %>
  </div>

  <% if @village.image.present? %>
   <span>目前图片</span>

   <%= image_tag(@village.image.thumb.url) %>
  <% end %>

  <div class="group">
    <%= f.input :image, as: :file %>
  </div>

  <div class="group">
    <%= f.input :description, as: :ckeditor, input_html: { ckeditor: { toolbar: 'mini' } }, :label => "内容" %>
  </div>


  <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>

<% end %>
